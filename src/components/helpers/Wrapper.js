const Wrapper = (props) => {
    // this is the component wrapper
    // new comment
    // new commit
    // I am adding here 
    // adding again here master
    return props.children;
};
export default Wrapper;
